[pytest]
testpaths =
    tests
markers =
    slow: end-to-end tests that invoke torch.compile or full training runs (deselect with '-m "not slow"').
    network: tests that query live remote services (deselect with '-m "not network"').
    legacy: tests of APIs deprecated for removal in kraken 8.
